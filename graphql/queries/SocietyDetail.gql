query society($slug: String!) {
  society(slug: $slug) {
    name
    description
    slug
    logo {
      url
    }
    banner {
      url
    }
    productions(orderBy: "start", first: 4) {
      pageInfo {
        hasNextPage
        endCursor
      }
      edges {
        node {
          name
          end
          isBookable
          slug
        }
      }
    }
  }
}
