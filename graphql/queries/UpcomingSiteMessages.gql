query upcomingSiteMessages(
  $afterCursor: String
  $displayLocation: SiteMessagesMessageDisplayLocationChoices
) {
  siteMessages(
    first: 5
    orderBy: "display_start"
    after: $afterCursor
    toDisplay: true
    displayLocation: $displayLocation
  ) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        title
        message
        active
        indefiniteOverride
        displayStart
        eventStart
        eventEnd
        eventDuration
        creator {
          id
          firstName
          lastName
        }
        type
        dismissalPolicy
        toDisplay
      }
    }
  }
}
