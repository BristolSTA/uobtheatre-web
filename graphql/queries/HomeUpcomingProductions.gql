query upcomingProductions($now: DateTime) {
  productions(first: 5, orderBy: "start", end_Gte: $now, status: "PUBLISHED") {
    edges {
      node {
        id
        name
        slug
        subtitle
        description
        featuredImage {
          url
        }
        coverImage {
          url
        }
        society {
          id
          name
          slug
        }
        start
        end
      }
    }
  }
}
