query upcomingProductions($now: DateTime) {
  productions(first: 5, orderBy: "start", end_Gte: $now) {
    edges {
      node {
        id
        name
        slug
        subtitle
        description
        featuredImage {
          url
        }
        coverImage {
          url
        }
        society {
          name
          slug
        }
        start
        end
      }
    }
  }
}
